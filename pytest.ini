[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short
markers =
    unit: fast, in-process unit tests (no network, no disk I/O)
    integration: slower tests that may use real files but no network
    golden: parity tests against captured golden values (require fixture data)
